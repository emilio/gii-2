class Utils {
	public static String desktopFile(String fileName) {
		// return System.getProperty("user.home") + "/Desktop/" + fileName;
        return System.getProperty("user.home") + "/Escritorio/" + fileName; // I'm on ubuntu!
	}
}
